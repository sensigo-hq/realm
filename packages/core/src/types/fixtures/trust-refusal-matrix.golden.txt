# buildTrustRefusal — cross-product golden (issue #508, final correction)
#
# For every (value × kind) pair below, the real classifyStepTrust verdict decides what
# renders: a `'refuse'` verdict renders all four (surface) cells through the real composer
# (step: 'work' throughout); `'gates'`/`'lawful_no_gate'` render one explicit marker line —
# no real call site ever reaches the composer for those, and rendering one anyway would be a
# refusal message about a value the engine accepts. This file is generated, not hand-edited
# — see trust-refusal-matrix.test.ts for the regeneration command and the reason this file
# exists: three prior rounds each found a NEW trust-message defect by probing whatever came
# to mind next; every one of those defects would have been a one-line diff HERE, the first
# time this file existed. A change to any cell below means a human must read this diff before
# it merges.
#
# 14 values × 4 kinds = 56 pairs;
# 49 of them are 'refuse' (× 4 surfaces = 196 rendered cells);
# 7 are 'gates'/'lawful_no_gate' (1 marker line each).
#
# NOTE on the 'refuse' cells that render: kind=guard, and kind=finalizer with a surface
# other than load, are cells no real call site constructs today for a NON-lawful value —
# guard's trust prohibition is a KIND prohibition that never reaches this composer at all,
# and dispatch/finding/briefing are structurally scoped to auto/agent by findEligibleSteps
# (see each real call site's own comment). They render here anyway, deliberately: this is a
# golden of the FUNCTION, not of reachability, so a future change that made one of these
# cells reachable would already have a pinned, reviewed answer waiting for it.

## value: auto (accepted)

### kind: auto (verdict: lawful_no_gate)
- (lawful — this pair never reaches buildTrustRefusal on any surface; no real call site would construct this cell)

### kind: agent (verdict: lawful_no_gate)
- (lawful — this pair never reaches buildTrustRefusal on any surface; no real call site would construct this cell)

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: "auto"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'auto'? Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "auto"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'auto'? Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "auto"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed; did you mean 'auto'? — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "auto"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'auto'? Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: lawful_no_gate)
- (lawful — this pair never reaches buildTrustRefusal on any surface; no real call site would construct this cell)

## value: human_confirmed (accepted)

### kind: auto (verdict: gates)
- (lawful — this pair never reaches buildTrustRefusal on any surface; no real call site would construct this cell)

### kind: agent (verdict: gates)
- (lawful — this pair never reaches buildTrustRefusal on any surface; no real call site would construct this cell)

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: "human_confirmed"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "human_confirmed"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "human_confirmed"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed; did you mean 'human_confirmed'? — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "human_confirmed"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: "human_confirmed"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: "human_confirmed"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: "human_confirmed"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: "human_confirmed"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.

## value: human_reviewed (accepted)

### kind: auto (verdict: gates)
- (lawful — this pair never reaches buildTrustRefusal on any surface; no real call site would construct this cell)

### kind: agent (verdict: gates)
- (lawful — this pair never reaches buildTrustRefusal on any surface; no real call site would construct this cell)

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: "human_reviewed"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_reviewed'? Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "human_reviewed"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_reviewed'? Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "human_reviewed"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed; did you mean 'human_reviewed'? — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "human_reviewed"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_reviewed'? Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: "human_reviewed"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: "human_reviewed"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: "human_reviewed"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: "human_reviewed"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.

## value: human_notified (retired)

### kind: auto (verdict: refuse)
- load: Step 'work': 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: agent (verdict: refuse)
- load: Step 'work': 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: "human_notified"' was removed (issue #508): it never triggered a notification, it never opened a gate, and most workflows should simply delete the key — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.

## value: human_confirmd (typo)

### kind: auto (verdict: refuse)
- load: Step 'work': 'trust: "human_confirmd"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "human_confirmd"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "human_confirmd"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed; did you mean 'human_confirmed'? — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "human_confirmd"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: agent (verdict: refuse)
- load: Step 'work': 'trust: "human_confirmd"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "human_confirmd"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "human_confirmd"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed; did you mean 'human_confirmed'? — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "human_confirmd"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: "human_confirmd"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "human_confirmd"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "human_confirmd"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed; did you mean 'human_confirmed'? — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "human_confirmd"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: "human_confirmd"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: "human_confirmd"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: "human_confirmd"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: "human_confirmd"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.

## value: Human_Confirmed (case variant)

### kind: auto (verdict: refuse)
- load: Step 'work': 'trust: "Human_Confirmed"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "Human_Confirmed"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "Human_Confirmed"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed; did you mean 'human_confirmed'? — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "Human_Confirmed"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: agent (verdict: refuse)
- load: Step 'work': 'trust: "Human_Confirmed"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "Human_Confirmed"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "Human_Confirmed"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed; did you mean 'human_confirmed'? — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "Human_Confirmed"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: "Human_Confirmed"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "Human_Confirmed"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "Human_Confirmed"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed; did you mean 'human_confirmed'? — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "Human_Confirmed"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Did you mean 'human_confirmed'? Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: "Human_Confirmed"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: "Human_Confirmed"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: "Human_Confirmed"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: "Human_Confirmed"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.

## value: engine_delivered (service)

### kind: auto (verdict: refuse)
- load: Step 'work': 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: agent (verdict: refuse)
- load: Step 'work': 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: "engine_delivered"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.

## value: engine_managed (service)

### kind: auto (verdict: refuse)
- load: Step 'work': 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: agent (verdict: refuse)
- load: Step 'work': 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: "engine_managed"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.

## value: agent_provided (service)

### kind: auto (verdict: refuse)
- load: Step 'work': 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: agent (verdict: refuse)
- load: Step 'work': 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: "agent_provided"' is a SERVICE's trust level (declared under 'services: <name>: trust:'), not a step's — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.

## value: nope (unrelated string)

### kind: auto (verdict: refuse)
- load: Step 'work': 'trust: "nope"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "nope"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "nope"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "nope"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: agent (verdict: refuse)
- load: Step 'work': 'trust: "nope"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "nope"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "nope"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "nope"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: "nope"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: "nope"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: "nope"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: "nope"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: "nope"' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: "nope"' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: "nope"' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: "nope"' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.

## value: "" (empty string)

### kind: auto (verdict: refuse)
- load: Step 'work': 'trust: ""' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: ""' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: ""' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: ""' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: agent (verdict: refuse)
- load: Step 'work': 'trust: ""' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: ""' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: ""' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: ""' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: ""' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: ""' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: ""' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: ""' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: ""' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: ""' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: ""' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: ""' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.

## value: null

### kind: auto (verdict: refuse)
- load: Step 'work': 'trust: null' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: null' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: null' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: null' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: agent (verdict: refuse)
- load: Step 'work': 'trust: null' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: null' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: null' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: null' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: null' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: null' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: null' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: null' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: null' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: null' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: null' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: null' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.

## value: 123 (number)

### kind: auto (verdict: refuse)
- load: Step 'work': 'trust: 123' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: 123' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: 123' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: 123' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: agent (verdict: refuse)
- load: Step 'work': 'trust: 123' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: 123' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: 123' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: 123' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: 123' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: 123' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: 123' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: 123' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: 123' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: 123' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: 123' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: 123' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.

## value: ["a","b"] (array)

### kind: auto (verdict: refuse)
- load: Step 'work': 'trust: ["a","b"]' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: ["a","b"]' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: ["a","b"]' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: ["a","b"]' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: agent (verdict: refuse)
- load: Step 'work': 'trust: ["a","b"]' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: ["a","b"]' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: ["a","b"]' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: ["a","b"]' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: guard (verdict: refuse)
- load: Step 'work': 'trust: ["a","b"]' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' — any run of this workflow picks up the corrected definition on its next attempt at this step.
- dispatch: Step 'work': 'trust: ["a","b"]' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Correct the value, then 'realm workflow register <path>' and retry this step — this run picks up the corrected definition.
- finding: 'trust: ["a","b"]' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). Accepts auto, human_confirmed, human_reviewed — correct the value and 'realm workflow register <path>'.
- briefing: 'trust: ["a","b"]' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. A step's 'trust' accepts auto, human_confirmed, human_reviewed. Do NOT call execute_step for it; report this to the user, who must correct the workflow definition and re-register it.

### kind: finalizer (verdict: refuse)
- load: Step 'work': 'trust: ["a","b"]' is not a recognized value — refused at load: this workflow cannot create a run while the value is wrong, so no step of it — gated or not — ever executes under it. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- dispatch: Step 'work': 'trust: ["a","b"]' is not a recognized value — refused at dispatch: no gate opens and this step does not run; this run is now parked, non-terminal, until the value is corrected, and any step depending on this one returns 'blocked' in the meantime. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- finding: 'trust: ["a","b"]' is not a recognized value — the engine will refuse this step at dispatch (VALIDATION_TRUST_VALUE). 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
- briefing: 'trust: ["a","b"]' is not a recognized value — calling execute_step here will be refused (VALIDATION_TRUST_VALUE): no gate opens and this step does not run; the run would then park, non-terminal, until the value is corrected, with any step depending on this one returning 'blocked'. 'trust' accepts auto, human_confirmed, human_reviewed, and only 'auto' is meaningful on execution: finalizer steps.
